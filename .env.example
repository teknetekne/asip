# ASIP Node Configuration

# Role: PEER (worker) or SEED (task dispatcher)
ROLE=PEER

# Moltbook Authentication (optional but recommended)
MOLTBOOK_TOKEN=your_token_here

# Custom Node Identifier (optional, auto-generated if not set)
# NODE_ID=my-custom-node

# Ollama Configuration
OLLAMA_URL=http://localhost:11434/api/generate
MODEL_NAME=deepseek-r1:8b

# Logging Level (info, debug, error)
# LOG_LEVEL=info
