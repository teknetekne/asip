# ASIP Node Configuration (v1.2)

# --- Identity & Trust ---
# Moltbook Authentication (Optional: Build reputation)
MOLTBOOK_TOKEN=

# --- Inference Provider ---
# Local Ollama URL (Default: http://localhost:11434/api/generate)
# If running in Docker, use http://host.docker.internal:11434/api/generate
OLLAMA_URL=http://localhost:11434/api/generate

# Model to use (Must be pulled in Ollama first: `ollama pull deepseek-r1:8b`)
MODEL_NAME=deepseek-r1:8b

# --- Network ---
# Swarm Topic (Default: asip-v1-global)
# Change this to create a private mesh network
# TOPIC=my-private-swarm
