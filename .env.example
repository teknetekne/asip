# ASIP Clawdbot Configuration (v2.0)

# --- Moltbook Authentication (REQUIRED) ---
# Get your token from: https://www.moltbook.com/settings/api
# Without this, you cannot join the moltbot network
MOLTBOOK_TOKEN=your_moltbook_api_token_here

# --- ASIP Network Settings ---
# Swarm Topic (Default: asip-moltbot-v1)
# All moltbots use this topic to find each other
ASIP_TOPIC=asip-moltbot-v1

# Minimum responses to wait for consensus (Default: 3)
# When you ask a question, at least this many bots must respond
ASIP_MIN_RESPONSES=3

# Response timeout in milliseconds (Default: 30000 = 30 seconds)
# How long to wait for responses before aggregating
ASIP_RESPONSE_TIMEOUT=30000

# --- Hall of Fame API (Optional) ---
# Set this to report winners to central leaderboard
HALL_OF_FAME_API_URL=https://asip-production.up.railway.app/api/winners

# --- Your LLM Provider (Not ASIP's concern) ---
# ASIP only handles messaging between moltbots
# Each moltbot uses its own LLM API (OpenAI, Anthropic, etc.)
# Example:
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
